# 组件封装

```tsx
// main.tsx
export default function App() {

    // 工具数据及工具组件
    const {
        read: {
            showVoteModal,
            showRecivedToast,
            isCountDownOpen
        },
        write: {
            setShowVoteModal,
            setShowRecivedToast
            setIsCountDownOpen
        }
        conponents: {
            VoteModal,
            RecivedToast,
            CountDown
        }
    } = useUITool()

    // 元数据及元组件
    const {
        read: {
            start,
            end,
            voteEnd,
            matchStart,
            selectedTeam,
            successTeam,
        },
        write: {
            setStart,
            setEnd,
            setVoteEnd,
            setMatchStart,
            setSelectedTeam,
            setSuccessTeam
            },
        conponents: {
            
        }
    } = useBase(isCountDownOpen)

    // 派生数据及派生组件
    const { 
        read: {
            isMatchEnd,
            statu,
            isVoted,
            canVote,
            hasTeamAdvanced,
            isSelectedTeamAdvanced,
        },
        components: {
            
        }
    }
    } = useDirectives()

    // 操作函数
    const {} = useMethods()



    return (
        
    )
}
```

